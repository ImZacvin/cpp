#include <stdio.h>
#include <stdlib.h>

int main() {
    int choice;
    int arr[100];

    scanf("%d", &choice);


}

void process(i, n, buffer) {
    if (i >= n) {
        return;
    }
    int test;
    scanf("%d", &test);

    
    scanf("%[^\n]", &str1);

    parse str1 here;

    if(str1.lengt != test) return;

    

    process(i + 1, n, buffer);

}

